# Daily Log — 2026-09-19

## Post-Stage-16: user acceptance testing found a real duplicate-data bug

With all 16 stages complete, the user started their own manual testing against the
now-running API/frontend. First thing checked — `GET /api/reference/property-types` — turned up
a real bug within minutes: "Apartment" and "Land" each appeared twice with different IDs.

**Root cause**: none of `ReferenceDataController`'s 7 taxonomy `POST` endpoints (property types,
subtypes, purposes, amenities, districts, municipalities, wards, localities) checked for an
existing row with the same name before inserting, and none of the underlying tables had a
uniqueness constraint either — so calling create twice with the same name silently produced two
rows. Checking every reference table found the same pattern had already happened, presumably
during earlier-stage manual testing before this build's current daily-log history began:
`PropertyTypes` (Apartment ×2, Land ×2), `Purposes` (Sale ×2), `Districts` (Kathmandu ×2),
`Municipalities` (Kathmandu Metropolitan City ×2, with its own duplicate Ward).

**Fix**: added an existence check to all 7 create endpoints, scoped to the right parent where one
exists (a district's uniqueness is scoped to its province, a municipality's to its district, a
ward number's to its municipality) or global for the flat taxonomies (property types, purposes,
amenities). `dotnet build` clean, 0 warnings/errors.

**Cleanup**: before touching any existing data, checked every duplicate pair's actual usage
(listings, demands, subtypes, child location rows) with RLS bypassed via
`sp_set_session_context`. In every single pair, one ID was genuinely in use and the other had
zero references anywhere — safe to delete. Confirmed with the user before running the deletes
(blocked once by the harness's own irreversible-action guard, re-run after explicit approval).
Deleted the 6 orphaned rows bottom-up (ward → municipality → district → 2 property types →
purpose). Verified live: `GET /api/reference/property-types` now returns exactly one "Apartment"
and one "Land"; a fresh `POST` attempting to recreate "Land" correctly gets rejected with `400`.
`rls_test.sql` rerun clean (11/11) after the direct database writes — no regression.

Restarted the running API in place to pick up the fix (killed the process from the prior
session's background task, relaunched with the same `REAK_JWT_KEY`).

---

## Dedicated tenant-isolation test pass, at the user's explicit request

The user asked for a structured cross-org isolation test: two ordinary member accounts (Org A =
Kathmandu Prime Properties, Org B = Pokhara Lakeside Realty), neither a system admin, testing
read/update/delete on another org's property, access to another org's users, and (N/A for this
app — no lease/tenancy entities exist) leases/tenants. Ran each test live with exact
endpoint/method/request/response documented.

**5 of 6 applicable tests passed clean**: reading (`404`), updating (`404`, DB-verified unchanged),
and deleting (`404`, DB-verified still exists) another org's property via a deliberately
non-shared (`NetworkVisibility: OwnerOnly`) listing; updating another org's `MemberEntities`
profile (`403`). Reading another org's `MemberEntities` profile returned `200` — confirmed
intentional, not a bug: that endpoint is explicitly the spec §4.2 public member directory,
documented as such in its own code comment, exposing only public contact info.

**1 real bug found**: `GET /api/profiles`, called by the ordinary Org B `MemberAdmin` account,
returned every profile in the system across all three orgs plus both system admins — email, full
name, last login, org memberships, and role assignments included. Root cause: gated only by
`RequirePermission("members.read")`, a slug shared between System-scoped roles (who should see
everyone) and the Organization-scoped `MemberAdmin` role (who shouldn't) — unlike
`MemberEntitiesController.Update`, which already explicitly checks `IsSystemAdmin` before a
cross-org action, this endpoint trusted the permission slug alone. Per the user's explicit
instruction, showed them the bug and a proposed fix before touching any code; fixed on their
go-ahead by scoping the query to the caller's own organization(s) unless they're a system admin.
Verified live: Org B now sees only its own single user; a system admin's view is unchanged (still
all 7 profiles). Committed as `b11e52d`.

This is a genuine finding Stage 15's regression pass did not catch — that pass exercised
`GET /api/profiles` only as a system admin (the natural account for an admin-only-looking
endpoint), never as an ordinary member, which is exactly the gap here.

## Follow-up sweep: same bug class, found twice more

Given the first finding was a systemic pattern (permission slug shared across system- and
org-scoped roles, no defense-in-depth check), searched for every other endpoint gated by
`members.read` and tested each live rather than trusting its own doc comments. Two more real
findings, one genuine pass, one RLS sanity re-check:

- **`GET /api/invitations` — real bug, same root cause.** An ordinary Org B `MemberAdmin` got
  back every invitation ever sent across all three orgs — invitee email, target org, role, who
  invited them. Fixed the same way as `ProfilesController`: scoped to invitations targeting an
  org the caller administers (via `ProfileRoleAssignments` where `Role.Name == "MemberAdmin"`),
  unless the caller is a system admin. Verified live: Org B now sees only its own org's single
  invitation; admin's view unchanged (still all 7).
- **`GET /api/membership-applications` — real bug, same root cause, simpler fix.** Same ordinary
  account read the full "admin review queue" — every applicant's company name, contact name,
  email, phone, private message, and internal review metadata. Since applications belong to no
  organization yet (that's the point — they're pre-approval), there's nothing to scope an
  ordinary member *to*; fixed by requiring `IsSystemAdmin` outright, matching
  `ReportsController`/`AuditLogsController`/`FeatureFlagsController`'s existing pattern for
  admin-only screens. Verified live: ordinary member now gets `403`; admin still gets `200`.
- **`GET /api/matches` (+ direct `GET /api/matches/{id}`) — genuine pass, not just untested
  trust.** `MatchesController`'s own doc comment claims correct scoping via `SessionContextOverride`
  + explicit ownership check; verified this empirically rather than taking the comment's word for
  it — created a real intra-Kathmandu match specifically uninvolved with Org B, confirmed Org B's
  list still showed only its own 6 matches (0 leaked) and a direct fetch of the excluded match
  correctly returned `403`. Archived the throwaway test demand afterward.
- **`GET /api/listings` — sanity re-check, pass.** Confirmed Org A's `OwnerOnly` listing never
  appears in Org B's list (RLS-backed, as expected).

`rls_test.sql` rerun clean (11/11) after both new fixes. Both fixes committed together in a
single commit (invitations + membership-applications share the same root cause and were found
in the same pass).

Also committed, separately, Stage 16's full documentation set (`README.md` + 8 files under
`docs/`, including the PDF run-guide from earlier in this session) — it had been written but
never actually committed before this session moved into isolation testing. Added a
`.gitattributes` (`*.pdf binary`) in the same commit after noticing this repo's
`core.autocrlf=true` would otherwise silently corrupt the binary PDF on any future checkout —
caught before committing, not after, by checking `git show :path | wc -c` against the working-tree
file's byte count.

---

## Extended isolation sweep — demands, workspace sub-resources, and everything else

Continued past the `members.read` family into every other mutation path and cross-cutting
concern in the API, testing live rather than reading code:

- **Demands** — the direct analog to properties, same full matrix: cross-org read of a
  default-`OwnerOnly` demand (`404`), read once shared to `AllMembers` (`200`, intentional), the
  demand's *contact* staying private even when the demand itself is shared (`null` fields), and
  cross-org update/delete (`403`, DB-verified nothing changed). All pass.
- **Notifications, saved listings, dashboard summary** — code-verified `ProfileId`/
  `MemberEntityId`-scoped already; confirmed live that each org/profile only ever sees its own
  counts and items. All pass.
- **`CmsController`** — entire class gated by a single `cms.manage` check ordinary roles don't
  hold; confirmed live (`403` on read and write) rather than just trusting the class-level
  attribute. Pass.
- **Collaboration workspace sub-resources** (`messages`/`files`/`activities`) — an uninvolved
  third org gets `200` with an empty list instead of the `404` the controller's own doc comment
  says non-participants should get everywhere. Investigated: RLS is genuinely filtering the rows
  (confirmed empty response bodies, not just empty-looking), so **no actual data crosses the
  boundary** — this is a status-code/enumeration inconsistency against the code's own stated
  intent, not a confirmed leak. Left as informational per the user's call not to chase it further
  right now; no fix applied.
- **Collaboration request accept/decline/cancel** — an uninvolved third org (not either party)
  tried all three against a real pending request between two other orgs. All `403`, DB-verified
  status unchanged. Pass.
- **`MatchRuleSetsController`** — admin-only (`match_rules.manage`, not granted to any org-scoped
  role); confirmed `403` live. Pass.
- **Match actions** (`POST /api/matches/{id}/actions`) and direct match fetch — an org with no
  stake in a real match got `403` on both. Pass.
- **Public anonymous endpoints** — direct-ID probe of a non-public listing (`404`); direct-ID
  probe of a listing that *is* flagged `isPublicVisible: true` while the `public_properties_
  enabled` feature flag itself is off (`404` — confirms the flag is enforced per-record, not just
  on the list endpoint). Pass.
- **Suspended org reachability** — temporarily suspended Stage15 Regression Realty, confirmed an
  ordinary member gets `404` reading it via the authenticated directory, reactivated immediately
  after. Pass.
- **Feature-flag writes and cross-org media deletion** — ordinary member `403` on both; DB-
  verified the targeted photo was never actually deleted. Pass.

Every test this round passed — no further fixes applied. Combined with the earlier rounds, this
covers every controller in the API and every mutation path reachable by an authenticated
non-admin account.

---

**Status**: All fixes from this session pushed to `origin/main` (`a67baa7..ae3b730`, six commits:
Stage 16 docs, reference-data duplicate fix, profile leak fix, invitations/membership-applications
leak fix, plus their daily-log entries). This session's final extended sweep found nothing further
to fix — nothing new to push.
